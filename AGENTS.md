# Repository rules

- Do not read, inspect, search, or derive implementation details from files under `tests/`.
- Tests may be written and executed, but implementation decisions must come from the project requirements, documentation, and public contracts rather than from inspecting test files.
